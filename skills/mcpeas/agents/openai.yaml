interface:
  display_name: "MCPeas"
  short_description: "Opinionated MCP app build standard"
  default_prompt: "Use $mcpeas as the default for MCP work: MCP servers, MCP Apps, ChatGPT apps, widgets, and major MCP refactors. Prefer the MCPeas standard unless a hard stop forces another stack."

policy:
  allow_implicit_invocation: false
