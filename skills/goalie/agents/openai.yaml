interface:
  display_name: "Goalie"
  short_description: "Turn rough ideas into clean Codex Goal text"
  default_prompt: "Use $goalie to turn this rough idea into clean Goal objective text without the /goal prefix."

policy:
  allow_implicit_invocation: false
