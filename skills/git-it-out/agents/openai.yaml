interface:
  display_name: "Git It Out"
  short_description: "Final verified mainline closeout"
  default_prompt: "Use $git-it-out to take this over completely: verify it, make proper commits, land it on main/mainline, push it, run repo-native release/deploy/tracker/Entire closeout, and ask only when a hard gate makes completion impossible."

policy:
  allow_implicit_invocation: true
